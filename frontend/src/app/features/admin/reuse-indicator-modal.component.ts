// Le composant modal a été retiré (vue interne de NewIndicatorChoiceModalComponent) - seule
// cette interface de résultat reste partagée.
export interface ReuseIndicatorResult {
  useGroupContext: boolean;
  contextFields: string[];
}
